services:
  - type: static_site
    name: blaze-business-os-app
    repo: https://github.com/Blaze9246/blaze-business-os
    branch: main
    buildCommand: cd frontend && npm install && npm run build
    publishPath: frontend/out
    autoDeploy: true
